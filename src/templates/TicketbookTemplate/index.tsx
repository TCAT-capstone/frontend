import React from 'react';

import Layout from '@styles/Layout';

import { TicketbookType } from '@src/types/ticketbook';
import {} from './style';

const TicketbookTemplate: React.FC = () => {
  return (
    <Layout>
      <span>티켓북 수정 페이지</span>
    </Layout>
  );
};

export default TicketbookTemplate;
